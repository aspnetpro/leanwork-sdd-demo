# Review: T-10 — Aplicar o brand guide Leanwork à tela

**Data:** 2026-08-14
**Plano:** `docs/plans/PLAN-001-encurtador-url.md`
**PRD:** `docs/prds/PRD-001-encurtador-url.md`
**Arquitetura:** `docs/architecture/proposta-arquitetural.md`
**Stack detectada:** .NET 10 / ASP.NET Core Razor Pages (fonte: CLAUDE.md do projeto + `Encurtador.Web.csproj`)
**Commit:** `061bf20` (T-10); ajustes de R-03/R-04 em `11d4b64`

## Escopo revisado

Arquivos alterados/removidos nesta tarefa:
- `src/Encurtador.Web/Pages/Shared/_Layout.cshtml` *(editado)*
- `src/Encurtador.Web/wwwroot/css/site.css` *(editado)*
- `src/Encurtador.Web/wwwroot/css/figtree.css` *(novo)*
- `src/Encurtador.Web/Pages/Shared/_Layout.cshtml.css` *(removido)*
- `src/Encurtador.Web/Pages/Shared/_ValidationScriptsPartial.cshtml` *(removido)*
- `src/Encurtador.Web/wwwroot/lib/**` *(removido, 60 arquivos)*
- `docs/plans/PLAN-001-encurtador-url.md` *(status, critérios, histórico)*

T-10 não declara `Implementa`/`Valida` (sem RN/CA) — é proposital, conforme o plano e a arquitetura §7.1 ("nenhuma regra de negócio depende dela").

## Eixo 1 — Aderência ao plano

- ✅ Paleta reduzida a `--preto`/`--branco`/escala de cinza via custom properties; nenhuma cor fora da paleta em `site.css`.
- ✅ Figtree aplicada como fonte primária (`font-family: 'Figtree', system-ui, sans-serif`), embarcada offline.
- ✅ Bootstrap removido do `_Layout.cshtml` (link CSS + scripts jQuery/Bootstrap), conforme risco explícito da tarefa.
- ✅ Nenhum framework CSS introduzido — CSS escrito à mão, ~130 linhas.
- ✅ **Escopo maior que o declarado, resolvido**: a lista original "Camadas/arquivos afetados" da tarefa citava só `site.css` e `_Layout.cshtml`. A implementação também removeu `wwwroot/lib/` (vendor Bootstrap/jQuery/jquery-validation), `_Layout.cshtml.css` e `_ValidationScriptsPartial.cshtml`. Autor confirmou a remoção (R-01, fechado) e o plano foi atualizado para listar os arquivos reais.

## Eixo 2 — Rastreabilidade

- N/A para RN/CA — tarefa sem decisão base, sem RN, sem CA, como o próprio plano documenta. Nada a rastrear além do ID `T-10` no histórico de execução (seção 11), que foi preenchido.

## Eixo 3 — Aderência ao spec (arquitetura)

- ✅ §3.2 (reprodutibilidade, "nada de dependência de rede em tempo de execução"): fonte embarcada via data URI em `wwwroot/css/figtree.css`, sem `@import` de CDN. Verificado por inspeção — não há nenhuma referência a `fonts.googleapis.com` ou domínio externo em nenhum `.cshtml`/`.css` do projeto.
- ✅ Meta de legibilidade ("poucos arquivos pequenos, ~120 linhas"): `site.css` tem 137 linhas mas é CSS declarativo, não código de produção C# — o guardrail do CLAUDE.md mira arquivos de código; ainda assim está na faixa.
- ✅ §7.1 (T-10 sem RN/CA, existe só por coerência visual de palco): confirmado, nenhuma regra de negócio tocada.

## Eixo 4 — Cobertura de teste

- ✅ Conforme o plano, "Testes a escrever: Não aplicável — apresentação" e a arquitetura §8 ("Não haverá teste de... renderização de view"). `dotnet test` continua com os mesmos 29 testes verdes — nenhuma regressão funcional.
- ✅ Verificação manual (não automatizada, como esperado): `dotnet run` + `curl` confirmam HTTP 200 em `/`, `/css/site.css`, `/css/figtree.css`; fluxo de POST válido devolve a URL curta completa; fluxo de POST inválido (`google.com`) reexibe o formulário com a mensagem de esquema preservada.
- ✅ **Critério de aceite fechado**: "O campo, o botão e a URL curta são legíveis a três metros de uma tela projetada" — confirmado pelo autor em 2026-08-14. Checkbox marcado no critério de aceite de T-10 e no ponto de validação humana da seção 9. Ver R-02.

## Eixo 5 — Qualidade do código

- ✅ Sem framework CSS, sem dependência nova.
- ✅ `html lang="en"` corrigido para `lang="pt-br"` no `_Layout.cshtml` — coerente com o conteúdo em português (CLAUDE.md do workspace).
- ✅ Título da página corrigido de `"@ViewData["Title"] - Encurtador.Web"` para `"@ViewData["Title"]"` — o valor anterior duplicaria "Encurtador de URL - Encurtador de URL" já que `Index.cshtml` define `ViewData["Title"] = "Encurtador de URL"`. Achado e corrigido durante a própria execução desta tarefa.
- 🔧 **Corrigido durante a review**: `button` não tinha `:focus-visible` monocromático — o anel de foco padrão do navegador (frequentemente azul) violaria "a tela usa apenas preto, branco e tons de cinza" para quem navega por teclado. Adicionado `button:focus-visible { outline: 3px solid var(--cinza-70); ... }`, espelhando o tratamento já existente em `input[type="text"]:focus`.
- ✅ R-03 (fechado): `Pages/Error.cshtml` tinha a classe `text-danger` do Bootstrap removido em `<h1>`/`<h2>`. Removida a classe (sem CSS por trás, o texto já renderizava como texto normal — a mudança é só higiene, não altera o visual). Confirmado por inspeção: `build` limpo, sem outra referência a `text-danger` no projeto.
- ✅ R-04 (fechado): `wwwroot/css/figtree.css` tinha 2 blocos `@font-face` em itálico (300–900) não usados por nenhum elemento da tela. Removidos (linhas finais do arquivo); arquivo caiu de ~84 KB para ~41,6 KB. `dotnet build`/`dotnet test` seguem verdes (29/29) e a fonte regular (normal, 300–900) usada pela tela permanece intacta.

## Achados

| ID | Severidade | Descrição | Status |
|----|-----------|-----------|--------|
| R-01 | Importante | Escopo da tarefa ampliado além da lista declarada de arquivos: remoção de `wwwroot/lib/`, `_Layout.cshtml.css` e `_ValidationScriptsPartial.cshtml`. Justificativa: ficaram inteiramente órfãos como consequência direta de remover as referências a Bootstrap/jQuery do `_Layout.cshtml`; mantê-los seria vendor code morto, contra o atributo de reprodutibilidade/legibilidade da arquitetura. Pede confirmação humana explícita por ser mudança fora do que o plano previu. | **Fechado** — autor confirmou a remoção; lista de "Camadas/arquivos afetados" de T-10 atualizada no plano para refletir o diff real |
| R-02 | Bloqueante (processo, não código) | Critério de aceite "legível a três metros" não verificado por um humano em tela real/projetor. Não há como um agente confirmar isso. | **Fechado** — autor confirmou a legibilidade em 2026-08-14; checkbox marcado no critério de aceite de T-10 e na seção 9 do plano |
| R-03 | Sugestão | `Error.cshtml` ainda referencia `text-danger` (classe Bootstrap morta). Fora do escopo declarado de T-10. | **Fechado** — classe removida de `<h1>`/`<h2>` em `Error.cshtml` |
| R-04 | Sugestão | `figtree.css` carrega itálico não usado (~42 KB desnecessários). | **Fechado** — os 2 blocos `@font-face` em itálico foram removidos; arquivo caiu para ~41,6 KB |

## Recomendação final

**✅ Aprovado.**

O código está correto, testado (build limpo, 29 testes verdes, fluxo ponta a ponta verificado via `curl`) e aderente à arquitetura (sem CDN, paleta estritamente monocromática, incluindo o ajuste de foco de teclado feito durante esta review).

1. **R-01 — Fechado.** Autor confirmou que a limpeza do vendor code (Bootstrap/jQuery/CSS isolado) órfão é aceitável; a lista de arquivos de T-10 no plano foi atualizada para refletir o diff real.
2. **R-02 — Fechado.** Autor confirmou a legibilidade a três metros em 2026-08-14.
3. **R-03 — Fechado.** Classe `text-danger` removida de `Error.cshtml`.
4. **R-04 — Fechado.** Blocos `@font-face` em itálico removidos de `figtree.css` (~84 KB → ~41,6 KB).

T-10 está definitivamente pronta, sem achados abertos.

# Review: T-12 — Escrever README com instruções de execução e matriz de rastreabilidade

**Data:** 2026-08-14
**Plano:** `docs/plans/PLAN-001-encurtador-url.md`
**PRD:** `docs/prds/PRD-001-encurtador-url.md`
**Arquitetura:** `docs/architecture/proposta-arquitetural.md`
**Stack detectada:** .NET 10 / ASP.NET Core Razor Pages + EF Core + SQLite, empacotamento Docker (fonte: CLAUDE.md do projeto + ADR-007 da arquitetura)
**Commit:** `d0803d3` (T-12)

## Escopo revisado

Arquivos alterados/criados nesta tarefa:
- `README.md` *(novo)*
- `docs/traceability/MATRIX-encurtador-url.md` *(novo/regenerado via skill `leanwork-trace`)*
- `docs/plans/PLAN-001-encurtador-url.md` *(status, critérios, histórico)*

T-12 não declara `Implementa`/`Valida` (sem RN/CA) — proposital: a tarefa entrega documentação, não regra de negócio. `Decisões base: ADR-008` (rastreabilidade auditável por busca textual).

## Eixo 1 — Aderência ao plano

- ⚠️ `Camadas/arquivos afetados` no plano declarava só `README.md`; a entrega real também inclui `docs/traceability/MATRIX-encurtador-url.md`, regenerado por completo (estava desatualizado, gerado quando só T-01 a T-03 existiam). A própria descrição da tarefa já previa isso ("O comando `/leanwork-trace` pode gerar a base da tabela"), então não é escopo não anunciado — é o campo de metadado da tarefa que ficou incompleto. Registrado como R-01 (Sugestão), sem impacto na entrega.
- ✅ Critério 1 — "uma pessoa com apenas Docker sobe o sistema seguindo só o README": confirmado. README traz `docker build -t encurtador . && docker run -p 8080:8080 encurtador` como comando único, seção "Como rodar" não depende de nenhum outro arquivo para o caminho feliz.
- ✅ Critério 2 — "a matriz cobre as 16 RNs e os 17 CAs, cada linha apontando para T e teste": confirmado por contagem direta — Tabela 1 da matriz tem 16 linhas (RN-01 a RN-16), Tabela 2 tem 17 linhas (CA-01 a CA-17) com nome de teste exato e arquivo.
- ✅ Critério 3 — "cada ADR ligada a pelo menos uma RN, com ressalva para ADR-001/ADR-008": confirmado. A lacuna é documentada tanto na matriz (seção "Lacunas identificadas") quanto no README (nota após a tabela de decisões arquiteturais), sem invenção de vínculo artificial. ADR-007 (materializada por T-11) tem a mesma natureza estrutural e foi corretamente adicionada à mesma ressalva em vez de forçada contra uma RN que não existe.

## Eixo 2 — Rastreabilidade

- ✅ Commit `d0803d3` referencia `T-12` na mensagem, com resumo do que README e matriz cobrem (convenção do projeto).
- ✅ Histórico de execução (seção 11 do plano) preenchido com evidência concreta (conteúdo do README, regeneração da matriz, teste real do bind mount).
- 🔴 **Inconsistência encontrada e corrigida nesta revisão:** a matriz gerada pelo próprio T-12 descrevia T-12 como `⬜ Pendente` na Tabela 3, no diagrama Mermaid e no cabeçalho — correto no instante em que foi gerada (a tarefa que a cria ainda não havia terminado), mas incorreto como artefato final publicado no mesmo commit que fecha a tarefa. Como o produto real deste repositório é a rastreabilidade em si, uma matriz que erra sobre o próprio estado do trabalho que a criou é um problema de credibilidade, não só um detalhe. Corrigido diretamente nesta revisão (Tabela 3, nó do diagrama, cabeçalho e contagem "onze/doze tarefas" na seção de lacunas) — ver R-02.

## Eixo 3 — Aderência ao spec (PRD + arquitetura)

- ✅ Arquitetura §3.2 (reprodutibilidade como atributo de qualidade prioritário): as instruções de bind mount para persistência (`ConnectionStrings__Default`) foram testadas de fato nesta sessão, em bash/WSL e em PowerShell — não documentadas por suposição. É o mesmo padrão de rigor que T-11 aplicou ao `docker build`/`docker run`.
- ✅ Arquitetura §9 (dívida técnica de persistência efêmera): README explica a dívida e a alternativa de bind mount sem prometer mais do que o sistema entrega (nenhuma menção a backup, rotação, ou volume nomeado — fora de escopo).
- ✅ PRD §4.2 (não-objetivos): README lista os sete não-objetivos com referência ao PRD, sem duplicar o texto completo — atende ao risco que o próprio plano registrou ("documento duplicado é documento que diverge").
- ✅ Arquitetura §3.3 (meta "cada ADR referenciada por pelo menos uma RN"): não fecha 100% ao pé da letra (ADR-001/007/008), mas o desvio é do tipo que a própria arquitetura antecipa como estrutural, e ambos os documentos (README e matriz) são honestos sobre isso em vez de mascarar.

## Eixo 4 — Cobertura de teste

- N/A conforme o plano: "Testes a escrever: Não aplicável — documentação." Verificação é por inspeção direta do conteúdo (contagem de RN/CA, teste dos comandos), o que foi feito nesta revisão e já havia sido feito na sessão de execução.

## Eixo 5 — Qualidade da documentação

- ✅ README não duplica o PRD: usa descrições truncadas + link por âncora (`docs/prds/PRD-001-encurtador-url.md#8-regras-de-negócio`).
- ✅ Estrutura de pastas no README bate com o repositório real (conferido por inspeção).
- ✅ Comandos de bind mount cobrem os dois ambientes de shell relevantes (bash/WSL e PowerShell) em vez de assumir um só — reduz risco de instrução quebrada para quem clonar em Windows.
- ✅ Seção "Reviews" do README é honesta sobre o histórico (cita os dois rounds com ressalva, T-01 e T-09, em vez de esconder que houve idas e vindas).
- 🟢 A matriz (antes da correção nesta revisão) tinha a inconsistência do Eixo 2 — já corrigida, ver R-02.

## Findings

Nenhum finding Bloqueante.

- **R-01 (Sugestão):** o campo `Camadas/arquivos afetados` de T-12 no plano deveria ter listado também `docs/traceability/MATRIX-encurtador-url.md`, já que a própria descrição da tarefa previa sua regeneração via `/leanwork-trace`. Não corrigido retroativamente no plano (reescrever histórico não paga o custo aqui) — registrado apenas como nota de higiene para tarefas de documentação futuras.
- **R-02 (Importante, resolvido nesta revisão):** `docs/traceability/MATRIX-encurtador-url.md`, no commit que a publica, ainda descrevia T-12 (a própria tarefa que a gerou) como pendente — Tabela 3, diagrama Mermaid e cabeçalho. Corrigido diretamente: status `✅ Concluída`, commit `d0803d3`, review referenciada, nó do diagrama e contagem de tarefas atualizados.

## Cobertura da tarefa

| Item | Esperado | Entregue | Status |
|------|----------|----------|--------|
| Decisões base (ADR) | ADR-008 | Rastreabilidade por busca textual mantida em README e matriz | ✅ |
| Critérios de aceite da tarefa | 3 | 3 atendidos | ✅ |
| Cobertura da matriz | 16 RN, 17 CA | 16/16 RN, 17/17 CA, cada um com T e teste | ✅ |
| Consistência interna da matriz | N/A no plano, mas implícito | 1 inconsistência encontrada e corrigida (R-02) | ✅ (pós-correção) |

## Conclusão

T-12 entrega o que o plano prometeu: um README que sobe o sistema só com Docker, e uma matriz de rastreabilidade regenerada e precisa cobrindo as 16 RN e os 17 CA do PRD, com a lacuna ADR-001/ADR-007/ADR-008 documentada honestamente em vez de forçada. O único problema real — a matriz descrevendo a si mesma incorretamente como tarefa pendente — é uma consequência natural de o artefato ter sido gerado antes do commit que o publica (mesmo problema estrutural do placeholder de hash de commit, já resolvido no histórico do plano para T-11) e foi corrigido diretamente nesta revisão.

Com isso, as 12 tarefas de `PLAN-001-encurtador-url.md` estão concluídas, revisadas e sem finding Bloqueante ou Importante em aberto em nenhuma delas.

**Recomendação final:** ✅ Aprovado, sem ressalvas (R-02 corrigido nesta própria revisão).

**Próximos passos sugeridos:**
- Nenhuma tarefa pendente no plano — encerrar o pipeline de execução de `PLAN-001-encurtador-url.md`.
- Rodar o smoke test manual da seção 6 do plano (fluxo completo em palco) e o item "Antes de fechar" da seção 9 antes da apresentação em 2026-08-15.
